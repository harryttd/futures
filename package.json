{
  "name": "coinbase-futures-monorepo",
  "private": true,
  "workspaces": [
    "coinbase-sdk",
    "backend",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces --parallel",
    "lint": "npm run lint --workspaces"
  }
}
